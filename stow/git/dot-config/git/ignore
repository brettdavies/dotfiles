# Environment variables
.env
.env.*
!**/.env.example

# AI Agents
.ai/
.claude/
.cursor/
# gbrain writes a per-repo source marker; never tracked.
.gbrain-source
**/CLAUDE.md
**/CLAUDE.*.md
**/AGENT.md
**/AGENT.*.md
**/AGENTS.md
**/AGENTS.*.md
**/.claude/settings.local.json

# Notes
.idea/

# Compound Engineering
**/todos/
**/.context/
**/TODOS.md
**/TODO.md
# docs/solutions is a symlink to ~/dev/solutions-docs in every brettdavies repo.
# Ignore it here so the symlink itself never appears in `git status`.
# The solutions-docs repo negates this rule with `!docs/solutions` in its own
# .gitignore so its contents remain tracked when working inside that repo.
docs/solutions

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Production builds
target/
build/
dist/

# OS generated files
.DS_Store
.DS_Store?
.Spotlight-V100
.Trashes

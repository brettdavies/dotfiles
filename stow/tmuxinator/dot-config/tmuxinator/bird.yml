name: bird
root: ~/dev/bird

windows:
  - main:
      layout: main-vertical
      panes:
        - yazi
        -
        - lazygit

post: |
  tmux resize-pane -t bird:main.1 -x "33%"
  tmux resize-pane -t bird:main.3 -y "33%"

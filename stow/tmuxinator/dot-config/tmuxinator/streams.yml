name: streams
root: ~/box/TX-AI/streams-control

windows:
  - main:
      layout: main-vertical
      panes:
        - yazi
        -
        - lazygit

post: |
  tmux resize-pane -t streams:main.1 -x "33%"
  tmux resize-pane -t streams:main.3 -y "33%"

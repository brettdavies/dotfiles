name: vault
root: ~/obsidian-vault

windows:
  - main:
      layout: main-vertical
      panes:
        - yazi
        -
        - lazygit

post: |
  tmux resize-pane -t vault:main.1 -x "33%"
  tmux resize-pane -t vault:main.3 -y "33%"
